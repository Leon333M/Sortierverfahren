// Dateimanager.h
#pragma once
#include <string>

class Dateimanager {
private:
    // Vaibalen
    std::string path;

public:
    // Funktionen
    Dateimanager();
    void exportMessData();

private:
    // Funktionen
};
